# CONTEXT.md

_Last updated: 2026-07-27 · branch: main (P6.5 landed, PR #69 merged) · session: P6 attention-anchored agent editor — autonomous stage loop_

## 1. What changed this session (P6.1 → P6.5, all merged)
- **P6.1 (#59, `5b22ea2`) — above-camera measure lag fixed.** `Block.tsx` sync a non-focused block's `innerText` in `useLayoutEffect` (before paint) → an above-camera programmatic grow no longer drift the camera. Load-bearing for P6 (an agent edit above the human caret is this exact flow).
- **P6.2 (#61, `9e65c93`) — spatial guard predicate + grace tracker (pure).** `src/agent/spatialGuard.ts` `guardedBlocks(input): Set<string>`; band ASYMMETRIC `[A-buffer, A+(visibleBlocks-1)+buffer]` (anchor = viewport TOP, visible span run DOWNWARD; defaults 4/4/120_000ms). Fail closed: awareness outage / unplaceable camera → guard all; pinned always; grace ~2min. `guardTracker.ts` `trackCameras` grace reducer.
- **P6.3 (#62, PR #64, `4f994be`) — commit-time spatial `ProposalGuard` at the authority.** `server/agent/spatialProposalGuard.ts` `createSpatialProposalGuard()`. `liveCameras` map awareness cameras via order+redirect; UNPLACEABLE → raw id → fail closed (NEVER `order[0]`). Signature = id+type+text DELTA; `guardedSpansIntact` = structural per-field compare over each guarded run. `proposeCommit.ts` widened ctx (awareness+now), `performance.now()` clock. Default-on (P4.5-safe).
- **P6.4 (#65, PR #67, `150db40`) — agent observer + cold-region LRU (pure selection).** `src/agent/coldScheduler.ts` LRU `selectNext`/`markWorked` (never-worked first, LRU, seeded from cold[0] so non-empty cold never null). `observer.ts` fold grace, `coldBlocks`, select. `spatialGuard.ts` export `resolveGraceMs` (single grace source). PBT with INDEPENDENT oracle (recompute guarded from raw inputs, never from observe's own tracked) — teeth mutation-verified.
- **P6.5 (#68, PR #69, `e28434a`) — native attention-anchored agent DRIVER (headless), closes #66.** `src/agent/attentionAgent.ts` `createAttentionAgent(peer, {actor?, config?, now?})` over `HeadlessPeer`: each `tick()` = resolve reader cameras → observe cold set → propose reorganize of the least-recently-worked cold block via `peer.propose` (fork-and-diff, NEVER self-apply) → advance LRU. Default actor `normalizeWhitespaceActor` (idempotent whitespace tidy); actor injectable for richer restructure. `src/agent/cameraResolve.ts` `resolveCameras(remotes, order, redirects)` — NON-laundering, read `.raw` not `.anchor`, mirror the authority `liveCameras` (#66 F-02). `observer.ts` refactored to one `ObserverState {tracked, lru}` + pre-resolved cameras (#66 F-06); `awarenessKnown = provider.awareness != null` (#66 F-07). Removed the dead `guardCamerasFromRemotes` laundering bridge. **#66 CLOSED** — the skipped laundered-camera pin is now a passing fail-closed test.

## 2. Decisions and why
- **Enforce at COMMIT, at the authority.** The single-threaded room authority evaluate the guard against the AUTHORITATIVE doc, closing the check-then-act race. Op-grain refuse.
- **Belt + suspenders.** Agent picks cold (`coldBlocks`, P6.4) = BELT; authority re-checks at commit (`guardedBlocks`, P6.3) = SUSPENDERS, for the race where a reader moved in after the agent read.
- **Fail closed everywhere.** null awareness / unplaceable camera / awareness outage → guard whole doc; dropped reader keeps band for `graceMs`.
- **LRU advance on ANY attempt (commit / refuse / throw).** P6.5 gate blocker: a throwing `actor` make `peer.propose` throw SYNC (headlessPeer run mutate sync before returning), so the advance was skipped → livelock re-picking the same target + cold-set starvation + split `ObserverState`. Fix = `try/catch/finally`: the `finally` advance the LRU always; a crash become a structured `TickResult`, never a rejected tick (the P6.6 loop must not wrap every tick).
- **Default-on but LATENT in prod.** Prod `server/index.ts` set no `authenticate` → proposal refused PRE-guard. Auth primitives (`mintPeerToken` CAP_PROPOSE, `createPeerAuthenticator`, `resolveRoomConfig`/`grantCaps`) ALL EXIST and wired in `peerMilestone.test.ts`. The driver now exists (P6.5); what's missing is the milestone e2e that publishes a human camera to PROVE the guard enforces (P6.6).

## 3. Tested / gated
- **253 pass / 2 skip**, tsc 0, build 0. 2 skips are honest #63 pins (P6.3 server awareness-freshness). The #66 skip is GONE (P6.5 un-skipped it as a passing test).
- Each stage gated: simplifier → 4 parallel `inspector`s (one lens each) → `adjudicator` (sabotage-verifies, renders determination) → fix → re-adjudicate → PR (`automerge`) → CI green → auto-merge. P6.5 landed PASS after a first-pass BLOCK (actor-throw livelock) fixed + re-adjudicated.

## 4. Carry-forward (adjudicator-logged, non-blocking)
- **P6.6 loop lifecycle (from P6.5 gate)** — [CF-1, GATING] `tick()` is NOT reentrancy-safe: `state` is read/written across the `await`, so overlapping un-awaited ticks last-write-wins clobber the fold (#66 F-06 undone). The P6.6 loop MUST land with a single-flight guard (`if (inFlight) return idle; try {…} finally {inFlight=false}`) OR a test proving sequential `await`. Fold agent **stop/destroy** here (no way today to halt in-flight/future ticks when the peer is torn down).
- **Agent driver polish (from P6.5 gate, all server-backstopped today)** — [CF-3] `pinned` not forwarded by the driver → add `pinned?: (doc) => Iterable<string>` to `AttentionAgentOptions` when a pinned source lands (server `spatialProposalGuard.ts:113` enforces it now). [CF-4] a clean cold block still round-trips a no-op proposal → short-circuit empty `encodeStateAsUpdate` in `propose`, or `ReorganizeActor → boolean`. [CF-2] `awarenessKnown` trigger is dead for a real provider (HocuspocusProvider always builds an Awareness) → the P6.6 loop should gate its start on `provider.synced` (whenReady already does); server-symmetric, grace protects live readers.
- **Server guard hardening (#63)** — [F-04] continuous awareness observer; [F-05] reconnect-gap empty-vs-unknown awareness + grace keyed by room name; [F-06] proposer self-exclusion. 2 skipped server tests.
- **Prior debt:** [I3-F2] shared-pool Postgres flake (green when DB up), [I3-F6] `sinkSocketError` private-`webSocket` cast.

## 5. Next step — P6.6 (P6 CLOSE, last phase)
**P6.6 — milestone e2e + provenance → DE-LATENT the guard, close P6.**
- **Milestone e2e (headless, real server):** wire `authenticate: createPeerAuthenticator(SECRET)` + an agent CAP_PROPOSE token (the `peerMilestone.test.ts` pattern), publish a HUMAN camera (`peer.lookAt`), run `createAttentionAgent` and PROVE the guard ENFORCES: an agent proposal into a COLD region COMMITS; a proposal into the human's camera band REFUSES op-grain. This is the first test with a live proposer + live camera — de-latents everything P6.2–6.5 built.
- **Loop + lifecycle:** a driving loop around `tick()` with the [CF-1] single-flight guard + stop/destroy.
- **Provenance rendering:** surface which blocks the agent authored (agent-touched runs).
- Then **P6 CLOSE** — the last phase; update the program status board.

Program status board (refreshed at phase boundaries): https://claude.ai/code/artifact/594fb42d-ae43-44e9-b903-acc5d33e9de2 — stale (still show P5); bump to P6 5/6 landed at next refresh.
